mixin toggle({title, name, status})
  .toggle
    label.toggle__label
      input.toggle__input(type='checkbox' name=name checked=status)
      span.toggle__button--outer
        span.toggle__button--inner
      span.toggle__text=title
